version: "2"
sql:
- engine: "postgresql"
  queries: "infra/postgresql/query.sql" 
  schema: "infra/postgresql/schema.sql"
  database:
    uri:  postgresql://postgres:root@localhost:5432/cart_go_db?sslmode=disable

  gen:
    go: 
      package: "rsqlc"
      out: "infra/rsqlc"
      sql_package : database/sql
      emit_db_tags: true
      emit_interface: true 
      emit_exact_table_names: true 
      emit_json_tags: true 
      emit_sql_as_comment: true 

  

  
  